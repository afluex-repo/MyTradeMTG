@model MyTradeMTG.Models.Admin

@{
    ViewBag.Title = "UserTopUpAllow";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@using (Html.BeginForm("UserTopUpAllow", "Admin", FormMethod.Post, new { @id = "form1", enctype = "multipart/form-data" }))
{
    <div class="container-fluid">
        <div class="row layout-top-spacing">
            <div class="col-lg-12 col-12 layout-spacing">
                <div class="card">
                    <div class="card-header">
                        <div class="row">
                            <div class="col-xl-12 col-md-12 col-sm-12 col-12">
                                <h4>User TopUp Allow</h4>
                                <span class="text-danger">
                                    @TempData["UserTopUpAllow"]
                                </span>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <label>User TopUp Allow</label>
                            <div class="col-md-3">
                                @Html.RadioButtonFor(m => m.IsActive, false) &nbsp;&nbsp;Self
                                 &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

                                @Html.RadioButtonFor(m => m.IsActive, true) &nbsp;&nbsp;All

                                @*<input type="radio" id="IsActive1" name="IsActive" value="0"/>&nbsp;&nbsp;Self&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                                <input type="radio" id="IsActive1" name="IsActive" value="1"/>&nbsp;&nbsp;All*@
                            </div>
                            <div class="col-md-3">
                                    <input class="btn btn-success" type="submit" value="Update" id="btnSubmit" name="Update" />                        
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
<script>
    $(function () {
        $("#btnSubmit").click(function () {
            debugger;
            if ($('input[type=radio][name=IsActive]:checked').length == 0) {
                alert("Please select atleast one");
                return false;
            }
            else {
                //alert("radio button selected value: ");
            }
        });
    });

</script>




